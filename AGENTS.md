# FormaBot 开发约束

## 工作顺序

遵循 requirements → design → implementation → testing → commit → push。
先读 README.md、docs/requirements.md 和 tasks/todo.md，其他文件按任务需要读取。
执行内核已决定使用 DeepSeek Harness，模块边界见 CAPABILITY-MAP.md。按 tasks/plan.md 先完成最简桌面 App：在 App 内配置真实模型、授权和执行任务，视觉打磨后置。安装包内置 Harness、运行时和必要浏览器组件，不要求用户补装。逐任务细化规格、设计和实现，不把选型决定当成运行验收。

## 产品不可丢失的约束

- 工作空间是默认执行和文件交付位置；成员具备 read/write/edit/bash/browser 能力。
- 首次实际执行前申请工作空间授权；同一工作空间持续复用，不按成员、任务或重启重复申请。
- 新成员继承工作空间已有基础授权；成员职责或内部边界不能转化为重复弹窗。
- 工作目录不是安全边界。文件、命令、浏览器和子进程的限制由执行层落实；不能仅依赖提示词。
- 云端模型配置面向普通用户：预置服务商和模型，选择并填写 API Key。
- 不把私聊和私人记忆默认注入群聊；具体共享策略设计时明确。
- 平台登录和工作空间授权是不同状态。账号过期可要求重新登录，但不能借此重复申请工作空间权限。

## 工程与交付

### 强制小步交付与用户手测

- 用户明确要求：每完成一个开发小阶段，都构建可运行 App 交给用户手动测试；自动测试通过不能代替用户验收。大任务拆为可独立验证的小切片，不积攒多个阶段后才交付。
- 每阶段交付必须说明：这一步做了什么、可运行构建的路径及版本/提交、用户要测试的具体步骤与预期结果、自动验证及已知限制、下一步做什么。
- 构建与必要检查成功后，更新 tasks/todo.md 为“待用户手测”，停在该阶段等待用户反馈。只有用户明确确认通过后才进入下一阶段；沉默、经过一段时间、没有收到报错均不算通过。
- 用户发现问题时先修复当前阶段、重新构建并提供针对性复测说明，再等待确认；不能带着反馈问题推进下一阶段。用户另行明确调整顺序时更新计划。
- 保留模型配置、工作空间、授权和用户数据；手测不要求重新配 Key 或选择空间。权限反例由开发者在隔离测试素材上验证，不让用户用重要目录或真实账号试险。
- 记录用户手测结论及构建标识，区分“实现/自动验证完成”“待用户手测”“用户已确认通过”。既往阶段没有明确用户反馈的，不补写为已手测通过。

- 保留用户改动，仅修改和提交本任务文件。验证通过后提交；有明确远程目标和推送授权才 push，不自行创建或发布 GitHub 仓库。
- 不提交真实 API Key、浏览器 profile、Cookie、用户附件或运行数据库。开发临时文件放 .tmp/。
- 重要权限行为需要真实执行验证；模拟测试、页面能打开、命令退出为零都不能替代产品验收。
- 每个阶段更新 tasks/todo.md，明确证据、剩余边界和下一步。
- 技术栈确定后补充实际命令和代码风格，不编造未存在的脚本。
- 本任务未授权向外发送、发布作品或扩大访问；开发发布功能时使用测试站点，真实外部操作需要对应授权。
