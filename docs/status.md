# 当前进度与验收状态

> 2026-09-12 审计提示：下方既有汇总存在过期完成状态和未关闭反例，最新审计基线 `e40838e` 请见 [仓库与开发进度审计](repository-audit-2026-09-12.md)。本轮未改写历史手测结论、未修复产品；依赖、交付版本、浏览器生命周期等不能依据旧通过记录视为关闭。

更新：2026-09-11。这是当前状态的唯一汇总入口；需求见 [requirements](requirements.md)，执行顺序见 [计划](../tasks/plan.md)，逐项进展见 [待办](../tasks/todo.md)。历史调研、设计目标和曾通过的成功用例不覆盖后来发现的失败反例。

## 产品定位与已确定事实

本地 AI 团队工作台；DeepSeek Harness 0.1.2-rc.1、TypeScript、Electron 44.2.0、包内 Node 26.7.0、node:sqlite。当前开发/实测平台 macOS arm64，未签名公证 .app；未证明干净机器和其他平台可交付。应用主进程持有 SQLite 和凭据，成员任务 sidecar 承载 Harness；应用持有对话、群与任务真相源。

App 已接通真实 DeepSeek v4-flash、五项工具、一次空间授权、加密配置保存、实际建队与群队列、三栏工作台及 HTML 预览。当前是一份全局模型配置、串行成员执行、每空间共享浏览器。独立成员模型、生图、完整上传下载、长期恢复、成员行为规范尚未完整实现。正式产品不可因“能运行一次”而验收。

## 验收矩阵

状态：局部实测 = 指定成功路径有证据；有缺陷 = 已有反例；未验收 = 缺少完整证据。没有将原型成功记录改写成正式产品通过。

| 验收 | 当前判定 | 证据/剩余门槛 |
| --- | --- | --- |
| A01–A03 配置、五工具、授权复用 | 局部实测 | [工作台证据](evidence/workbench.md)；覆盖拒绝、新成员、重启的已有范围，不包含全部失效情形 |
| A04–A05 撤销与执行边界 | 局部实测 | **E02 已关闭**（2026-09-12）：F01 硬链接打断、F02 工具级残余回收、E02b-1 出站网络仅回环（逃逸影响收敛）、E02b-2 撤销/授权语义修正（浏览与执行解耦）。detached 逃逸为记录在案的已知边界，容器评审放在 E10 前；F03 逐动作审批属 E03b |
| A06 多模型 | 未完整验收 | 仅一服务商真实任务证据，预置列表不代表可用；成员独立选模型待做 |
| A07 分工/交接 | 局部实测、有缺陷 | [群协作证据](evidence/group-and-preview.md)；E01 已修根状态一致性；E06a 中断任务可重新入队（用户确认后）；E06b 依赖派发（depends_on，前置受阻下游不派发）真实验证通过；E07a-1/2 交付快照（旧链接保留原版本，含 HTML 关联资源首读固定）已验证；验收事实账本深化与审批 E06c 待做 |
| A08 人类控制 | 局部实测、有缺陷 | @与追加入队有证据；E01 已修人工 FIFO 和会话误合并；当前纠正、完整停止仍见 F02/F04 |
| A09 信息/账号边界 | 局部实测、有缺陷 | 私聊不默认注入；E03a 人类接管租约与 E03b-1 逐动作审批（拒绝/放行一次/始终允许规则化）真实验证通过；账号范围 F07 尚缺；R31 敏感值安全填入已真实验证（用户亲自输入、对话零泄漏），规则管理界面已交付 |
| A10 登录与双身份 | 未完整验收 | Cookie 持久化不代表身份隔离，平台登录接管待做 |
| A11 产物 | 局部实测、有缺陷 | 预览/源码复制有证据；失败登记、历史版本 F08 待修 |
| A12 恢复/幂等 | 未验收 | F11；中断标记不是可恢复任务账本 |
| A13 生图 | 未实现/未验收 | 选型参考不能代替真实图片交付 |
| A14 文档建队 | 局部实测 | 自然语言五岗位已建队；文档驱动审批节点、职责变更与一致性 F10 待补 |
| A15 安装交付 | 局部实测 | 本机自包含 .app；干净机器、签名、公证未验收 |
| A16 工作台/预览 | 局部实测 | 三栏、抽屉、尺寸、HTML 首次打开及浏览器导航；复杂资源待补 |
| A19 模型连接提示 | 局部实测 | 单模型提示及失败判断已有证据；独立成员模型标签待 E09a |
| A17–A18 成员行为 | 局部实测、部分验收 | E04c 已交付显式静默协议（关键词判定退役、模型自主判断），真实模型群聊链路验证通过；防刷屏与全员送达的注意力骨架 E04d 已交付（广播漏协调人已修复，真实模型验证）；E05b 已完成：聊天改岗请求+用户批准（user_approved 版本持久化）与协调人持久转移（群设置操作，重启保留） |

## 已知缺陷与使用边界

审查 F01–F12 的原始基线和 JSON 保持不改。整改结果另写证据，只有对应反例转为安全预期且通过，才关闭该项。当前不要扩大到重要真实目录、真实账号和自主发布验收；继续使用隔离构造素材与回环站点。工作空间基础授权仍只请求一次，修复不得用反复弹窗代替执行检查。

源码存在、单测通过、模型自述和实际产品验收是不同等级的证据。当前整改完成项实时写入 [待办](../tasks/todo.md)，完整验收状态在各阶段结束后更新此矩阵。首次整改的状态/排队修复也不能关闭 F01–F07 整组问题。

## 最新整改证据

E00 文档统一完成；E01 状态/排序/会话边界切片完成。[验收记录](evidence/e01-control.md) 含 18 项测试、打包版真实 deepseek-v4-flash 三种结果与两成员协作回归、重启和授权复用。F04/F05 仅关闭其中已验证子问题；其余 F01–F12 按计划继续，防刷屏后续已有E04a限定切片，持久改岗仍未实现。

沟通覆盖复核补充：[要求追踪表](conversation-coverage.md)。产出目录当前主要固定 outputs，App 自选目录尚未完整验收；文档输入到真实建队的普通用户完整路径待 E09e；R22–R24/A19 已加入计划，没有增加实现完成声明。

## 新增扩展能力范围

R25–R27/A20–A22/E11分片推进：设置工具清单已交付且用户已看到；Skill 启停与固定副本按需读取已实现，120项测试和真实模型方法/脚本/文件交付验证通过，待用户手测。通用 MCP 客户端、插件安装生命周期仍未实现/验收，不能将发现或Skill文本读取当作全部扩展可用。详见 [设计](capability-management.md)；完整发行验收扩展至 A01–A22。

## UI-01 分支开发


首版视觉被用户拒绝，第二稿已改为灰白轻量工作台并构建；类型检查、18项测试、浏览器/预览回归、真实模型三种结果通过。当前仍待用户视觉及手测验收，未合并，见 [UI-01返工证据](evidence/ui-01.md)。

UI-01e 统一群聊工作台已构建，双侧栏回归、群聊UI和真实模型回归通过，仍待用户手测、未合并。具体证据与手测步骤见 tasks/todo.md 的 UI-01e。

E04a反馈切片已构建待手测：@候选、Markdown渲染、产物右侧链接、重复展示治理；不硬限制Bot回复字数/次数。DeepSeek Vision预置已加，图片传入/识图未完成。见 [证据](evidence/e04a-public-replies.md)。

E04b已构建待手测：模型可按完整语义通知本群全员；DeepSeek服务端web_search流式请求真实通过；私聊不能跨群发送/派单。见 [证据](evidence/e04b-group-search.md)。持久任命未实现，研究来源相关性和结论严谨性尚未通过业务验收，不把链路通过等同内容正确。

## 2026-09-10 后端专项研究

已完成[官方/开源/本地代码核验](backend-research-2026-09-10.md)、[后端方案](backend-architecture.md)及[更新/浏览器选区设计](updates-and-browser-selection.md)，不代表功能实现。E05a已构建待用户手测，下一片E05b需用户确认后推进，顺序以plan最新节为准，E02/E03边界门槛仍有效。

此前功能构建为menu-01（277700a），包括右键菜单、重名拦截和确认删除等局部功能；前端按用户意见暂停扩展。A23更新、A24选区均未实现/未验收；没有签名更新源和实际图像输入证据。30项既有测试不能覆盖未来后端目标。本轮只更新文档，不构建伪装新版本。


## E05a 稳定身份与角色版本

最新构建 `build/e05a-ui-fix/mac-arm64/FormaBot.app`，已实现并完成自动/真实模型验证，**等待用户手测**。[设计与证据](e05a-stable-identity.md)：改名不改变作者 ID，职责版本和迁移备份持久化，任务启动时固定当前版本，编辑页可查看历史。34项测试、打包UI、生产副本迁移及两组真实模型角色验证通过；模型/Key/空间/授权复用。既有模糊历史保持未确认。手动编辑职责已版本化，聊天自动改岗/权威关系 E05b 与运行中变更拦截 E06c 尚未实施。

这只关闭稳定身份、版本与消息归属子项，不关闭职责守界、语义回应或产出质量。真实复盘仍出现由汇总指标推断掉点位置、引流因果的无依据结论，E07保留具体反例。A23更新/A24选区与原安全门槛未改变；未合并、未推送。

E05a-UI已修复用户反馈的旧消息竖排提示：后台核验状态保留，聊天移除可见身份警示，仅名称悬浮说明旧记录缺少信息。类型检查与打包UI回归通过，证据 `.tmp/ui-review-yRnvTD`；等待用户手测，不改变E05a后端验收边界。

## 2026-09-12 审计后 S1a 与执行过程 UI（待用户手测）

完成 @ 标点/全角识别、依赖绑定具体任务、循环拒绝、事务入队和失败链收敛；当前 Bot 名字下方显示单行浅色真实执行步骤、动作图标及扫光，底部过程隐藏。类型检查、86 项测试及独立打包完成。构建 `build/s1a/mac-arm64/FormaBot.app`（0.1.0）。详细证据见 `docs/evidence/task-control-s1a.md`。真实模型直接 @ 产出成功；缺失素材场景未覆盖依赖账本路径，不能记为该路径真实验证通过。

全员静默判断/去除默认主管仍待 S1b 实现，当前没有宣称完成新群聊模式。之后依次推进可信交付、浏览器与审批、受控内测、正式发行；未发布、无远程推送目标。手测重点：私聊和群聊执行时，名字下方只有一条浅色步骤随真实动作切换，图标对应动作、轻微扫光，完成后收起，不在工作区底部出现执行过程。

## 2026-09-12 产物时间轴与原群返工（待用户手测）

已完成当前 Bot 完整登记产物时间轴与双入口预览、跨会话预览清空、实际执行会话归属修复、原任务定位返工、原群 @ 原交付者、逐级复核与原 Bot 私聊回报。跨群必要包沿用审批；私聊图片不随返工传入群。返工工具成功后执行层主动让出回合，防止模型等待阻塞上游。产物快照统一成员任务编号，Bash 产物可通过结果声明登记。

类型检查、90 项测试、最终打包 UI 回归 `.tmp/ui-review-F68kJW` 通过；真实模型 `.tmp/live-rework-CHb0aV` 验证编辑在原群修改文件、主管实际读文件复核、原 Bot 私聊回报、私聊无上游执行气泡，且修改工作文件后历史预览仍为交付快照。构建 `build/s1a/mac-arm64/FormaBot.app`（0.1.0，提交及摘要见 build/s1a/build-info.json）。详细实现、证据和手测步骤见 `docs/artifact-rework-slice.md`。

既往未知归属/时间/快照不伪造；重启不自动恢复整条返工复核链。完整全员静默判断及其他上线门槛仍未关闭。请先复测本片两项用户流程。

## 2026-09-12 最新用户反馈切片

U01–U14 最新状态以 [需求总账](requirements-review-2026-09-12.md) 为准；轻量内嵌产物、原生设置、固定项目空间已实现并自动验证，待用户手测，见 [交付与边界](scoped-workspaces-slice.md)。全员静默判断仍未实现；未进入受控内测或正式发行。
