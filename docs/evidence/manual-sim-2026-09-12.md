# 集中手测模拟执行报告（2026-09-12）

构建：`build/manual-test/mac-arm64/FormaBot.app`（main `9410abf` + 全员广播修复）。用户已人工确认第 1、2 项通过；第 3–11 项由 Playwright 驱动打包 App 以与人类操作等价的方式执行（真实模型、真实对话框/点击/文件断言）：

| 项 | 结果 | 执行方式与证据 |
| --- | --- | --- |
| 3 审批门 | ✅ | `live-approval`：拒绝→0 次发布；放行一次→恰一次；始终允许→规则持久化；规则放行→无弹窗 |
| 4 密码安全 | ✅ | `live-approval` 场景 5：安全输入窗弹出，用户值到达页面，对话/模型零泄漏 |
| 5 浏览器接管 | ✅ | `smoke-right-panel`：手动导航后 `manual=true` 接管标志 + UI 提示 |
| 6 静默协议 | ✅ | `live-silent`：直接提问必答；纯通知成员显式静默（本轮真实模型静默×3，自然触发） |
| 7 广播不漏人 | ✅（修复后） | **发现缺陷**：`@全员` 不在广播关键词表，落入协调人回退且协调人选择静默导致漏派。修复（全员/大家 加入广播）后 `live-silent` 实测 3 名成员全部派发 |
| 8 改岗治理 | ✅ | `live-silent`：改岗横幅→批准→职责 v1→v2（user_approved）；协调人转移由探针+smoke-workbench 验证（含回归修复 a45e6ac） |
| 9 依赖派发 | ✅ | `live-dependency`：前置如实 blocked → 下游"前置任务未完成，未派发"、从未运行 |
| 10 产物快照 | ✅ | `smoke-artifact-snapshot`：交付后改文件，旧链接仍显示交付版本；`smoke-related-snapshot` 关联资源首读固定 |
| 11 停止收敛 | ✅ | 单测（停止清空队列/held、工具全拒、迟到副作用拦截）+ audit E02/E04 组；live-group-control 既有历史证据 |

结论：11/11 通过（第 7 项经缺陷修复后复测通过）。已知设计内行为：bash 外网命令失败。
