# UI-01 返工版：待用户视觉与手测验收

2026-09-09，分支 `rouice/ui-workbench`。首版被用户明确拒绝；本次为重做后的第二稿，未合并 main。不是产品工程化整体完成。

## 改动


配置、权限、执行内核与 IPC 未改。长文折叠只是显示处理，不是 R20 的防刷屏/思考过滤实现。

## 验证

- `npm run typecheck` 通过；`npm test` 7 文件 18 项通过。
- `npm run package:dir -- --config.directories.output=build/ui-01` 通过。
- `node scripts/smoke-ui-review.mjs`：真实窗口里的构造素材，名称/职责搜索、无结果、长文展开、HTML预览、1440/800窗口、发送停止同一行、设置返回通过。修正旧截图测试过早截图的问题，等待实际控件边界稳定。
- `node scripts/smoke-workbench.mjs`：新建 Bot/群、成员选择、拖拽与抽屉、重启后加密配置保留通过，证据 `.tmp/workbench-ui-5dC5Mb/workbench.png`。
- 打包版 `smoke-right-panel.mjs --packaged`：HTML首次打开、脚本交互与隔离、源码/复制、浏览器地址/后退/前进/刷新通过。
- 打包版 `live-outcomes.mjs`：真实 deepseek-v4-flash 的 responded/blocked/completed 三种结果、实际写入并核对 outputs/check.txt、跨会话拒绝及草稿保留、授权不重复、重启状态保留通过。用隔离工作空间与既有加密模型配置，无生产会话改动或真实平台发布。

证据目录（本机开发临时证据，不含于提交）：
- `.tmp/ui-review-o9003o/`：三栏宽窄截图、会话、设置；截图文字是明确标记的排版样例，不是模型产出。
- `.tmp/right-panel-jsROEI/preview.png`
- `.tmp/live-outcomes-OFijm6/`：真实模型三个状态截图。

## 用户测试

测试版：`build/ui-01/mac-arm64/FormaBot.app`，未签名开发版。先退出旧版再打开；继续复用已有模型/空间配置，不要求重新填 Key 或选空间。

1. 先审整体布局、灰白风格、图标和输入框；不接受则继续本分支返工。
2. 搜索并切换成员/群组；新建一个测试 Bot/群组，预期左侧真实出现且可打开。
3. 拖动左右分割线，收起/展开，再缩小窗口；预期输入、发送和预览按钮完整可操作。
4. 打开已有 HTML 产物，切换源码/预览并复制；打开浏览器，检查地址栏与导航按钮。
5. 用已配模型执行一个小文件任务，预期真实模型状态、回复与文件可见；退出重启，模型和工作空间仍在。

用户明确通过后合并 main，再按小步规则恢复 E02；未通过不合并。E02–E11 已知权限/停止、角色、模型扩展与交付缺陷仍按原计划处理。
